class Classifier:
    def __init__(self):
        return

    def fit(self, X, y):
        """
        Fits the model to the training data
        :param X: A list of 2D numpy matrices representing images
        :param y: A list of their corresponding classes (Can pass null if doing unsupervised learning)
        :return: (Optional)
        """
        return

    def predict(self, test_set):
        """
        Predicts the classes of images corresponding to the test set using
        the parameters of the model
        :param test_set: A list of 2D numpy matrices representing images
        :return: A list of predicted classes to the test set
        """
        return []
